# §5.9/§5.10 dev-only fixture: `callback_secret_ref` (a caller-supplied
# reference, never the raw secret itself — POST /v1/agent/jobs's own
# body) resolves to an actual HMAC signing key here. A real deployment
# would resolve this through whatever secrets manager issued the ref to
# the tenant in the first place (§7.2); for the POC there's no such
# registration flow, so this file stands in for it — read by
# `async-worker` only (`webhook.py`), never by gateway or harness.
secret_ref_abc: duta-dev-webhook-secret-never-use-in-production
